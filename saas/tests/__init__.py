"""Tests for the boonyardnn SaaS layer (Phase 2, slice 1).

Run from the repo root::

    python -m unittest discover -s saas/tests -t saas

The package's own suite under ``tests/`` is discovered separately and is not
touched by anything here.
"""
