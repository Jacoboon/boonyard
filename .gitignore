__pycache__/
*.py[cod]
.ruff_cache/
.coverage
htmlcov/
dist/
*.egg-info/
# Boonyard's own live node (M8) — the node is a live artifact; data never goes in git.
# (gitignore has no inline comments — this must be on its own line to work.)
node/
*.db
*.db-wal
*.db-shm
